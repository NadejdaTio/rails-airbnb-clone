<h1>Liste des membres</h1>
  <% @profiles.each do |profile| %>
    <div>
    <%= profile.id%>
    <%= profile.first_name%>
    <%= profile.last_name%>
    <%= profile.phone_number%>
    <%= profile.address%>
  </div>
<% end %>
